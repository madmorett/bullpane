node_modules
dist
.env
# qualquer .env real (.env.trial, .env.production, ...), mas mantendo os templates
.env.*
!.env.example
!.env.company.example
*.local
.DS_Store
coverage
.turbo
apps/web/dist
apps/server/public
keys/
*.tsbuildinfo

# --- Nada aqui vai para o git ---
# Configuração de clientes (Monest incluída), chaves de licença assinadas,
# endpoints e IPs reais. O repositório pode virar open source; esta pasta não.
private/
keys/
*.pem

# cache local do `wrangler dev` (miniflare)
apps/website/.wrangler/
